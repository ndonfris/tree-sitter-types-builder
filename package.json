{
  "name": "tree-sitter-types-builder",
  "version": "0.0.1",
  "description": "",
  "main": "./out/index.js",
  "bin": {
    "tree-sitter-types-builder": "./bin/tree-sitter-types-builder"
  },
  "scripts": {
    "clean": "rimraf out node_modules",
    "build": "pnpm run clean && pnpm install && pnpm compile",
    "compile": "npx tsc -b"
  },
  "keywords": [
    "tree-sitter",
    "parser",
    "typescript"
  ],
  "author": "ndonfris",
  "license": "ISC",
  "devDependencies": {
    "@esdmr/tree-sitter-fish": "^3.5.1",
    "@types/node": "^20.11.30",
    "tree-sitter-cli": "^0.20.8",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "tree-sitter": "^0.21.0",
    "web-tree-sitter": "^0.22.2"
  }
}
